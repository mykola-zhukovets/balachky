; ЗГЕНЕРОВАНО з whisper_core.__version__ — не редагувати вручну.
; Оновлення (з кореня репо, див. docs\BUILD.md):
;   .venv\Scripts\python -c "from whisper_core import __version__ as v; print(f'#define AppVersion \"{v}\"')" > installer\version.iss
#define AppVersion "1.2.3"
